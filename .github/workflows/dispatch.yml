name: "Workflow Dispatch"
on: push
concurrency: 
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true


jobs:

  call-lint:
    name: "Lint"
    uses: ./.github/workflows/lint.yml
    secrets: inherit

  call-build:
    name: "Build"
    needs: call-lint
    uses: ./.github/workflows/build.yml
    secrets: inherit

  call-build-test-image:
    name: "Build test image"
    needs: call-build
    uses: ./.github/workflows/build-test-image.yml
    secrets: inherit

  call-test:
    name: "Tests"
    needs: call-build
    uses: ./.github/workflows/test.yml
    secrets: inherit

  call-release:
    name: "Release"
    needs: call-test
    if: ${{ github.ref_protected == true }}
    uses: ./.github/workflows/release.yml
    secrets: inherit

  # this one will be called after release issuing. Let's uncomment it after successfull review   
  # call-build-test-image:
  #   name: "Build test image"
  #   needs: call-release
  #   uses: ./.github/workflows/build-test-image.yml
  #   secrets: inherit