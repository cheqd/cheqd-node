version: '3.2'

services:
  validator-0:
    image: "${CHEQD_IMAGE_NAME}"
    ports:
      - "26656:26656" # p2p
      - "26657:26657" # rpc
    volumes:
      - "${MOUNT_POINT}/network-config/validator-0:/home/cheqd/.cheqdnode"
    command: start

  validator-1:
    image: "${CHEQD_IMAGE_NAME}"
    ports:
      - "26659:26656" # p2p
      - "26660:26657" # rpc
    volumes:
      - "${MOUNT_POINT}/network-config/validator-1:/home/cheqd/.cheqdnode"
    command: start

  validator-2:
    image: "${CHEQD_IMAGE_NAME}"
    ports:
      - "26662:26656" # p2p
      - "26663:26657" # rpc
    volumes:
      - "${MOUNT_POINT}/network-config/validator-2:/home/cheqd/.cheqdnode"
    command: start

  validator-3:
    image: "${CHEQD_IMAGE_NAME}"
    ports:
      - "26665:26656" # p2p
      - "26666:26657" # rpc
    volumes:
      - "${MOUNT_POINT}/network-config/validator-3:/home/cheqd/.cheqdnode"
    command: start

  seed-0:
    image: "${CHEQD_IMAGE_NAME}"
    ports:
      - "26668:26656" # p2p
      - "26669:26657" # rpc
    volumes:
      - "${MOUNT_POINT}/network-config/seed-0:/home/cheqd/.cheqdnode"
    command: start

  observer-0:
    image: "${CHEQD_IMAGE_NAME}"
    ports:
      - "26671:26656" # p2p
      - "26672:26657" # rpc
    volumes:
      - "${MOUNT_POINT}/network-config/observer-0:/home/cheqd/.cheqdnode"
    command: start
